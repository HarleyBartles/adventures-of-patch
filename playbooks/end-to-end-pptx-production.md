# End-to-end PPTX production playbook

This playbook has moved.

Canonical source:

```text
../docs/project/playbooks/end-to-end-pptx-production.md
```

This root-level file exists as the first-class discovery pointer while the project migrates playbooks to root `/playbooks/`.

Do not edit this pointer as the canonical playbook body. Until the migration is completed in a dedicated pass, read the canonical body at `docs/project/playbooks/end-to-end-pptx-production.md`.

Future target state:

- root `/playbooks/end-to-end-pptx-production.md` contains the canonical body;
- `docs/project/playbooks/` is removed or becomes a compatibility pointer;
- all repo, skill, and issue references are updated to the root path.
