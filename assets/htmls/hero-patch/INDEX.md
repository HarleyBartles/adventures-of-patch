# assets/htmls/hero-patch index

This folder mirrors `assets/hero-patch/` for self-contained HTML inspection packages.

The current proof package is `asset_class__hero__patch_approved_style.html`.

The canonical source image is `assets/hero-patch/asset_class__hero__patch_approved_style.png`.

The HTML package is not canon and must be regenerated if source metadata changes.

## Files in this directory

- `asset_class__hero__patch_approved_style.html` - self-contained GPT visual bridge proof for the canonical Patch contact-sheet image.

## Subdirectories

None.
